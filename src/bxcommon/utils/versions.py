# Current bloXroute protocol version. If the protocol version changes, it MUST be changed here
BX_PROTOCOL_VERSION = 1


# Get the bloXroute protocol version
def get_protocol_version():
    return BX_PROTOCOL_VERSION
